# 自动扩缩容(NFR-1:支撑 500 并发)
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: assets-api
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: assets-api
  minReplicas: 3
  maxReplicas: 20
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
