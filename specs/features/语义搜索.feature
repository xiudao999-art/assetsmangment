# language: zh-CN
功能: 语义搜索
  文本/图搜物料,向量近邻 + 元数据过滤 + hybrid。

  @REQ-301
  场景: 文本查询按相似度返回
    假如 库中有若干已审核通过的物料
    当 我用文本查询
    那么 应生成 embedding 做向量近邻检索
    而且 结果应按相似度排序返回

  @REQ-302
  场景: 专有名词用 hybrid 命中
    假如 库中有含特定专有名词的物料
    当 我用该专有名词查询
    那么 应使用 hybrid 向量加BM25 检索
    而且 该物料应命中

  @REQ-303
  场景: 违规物料不出现在搜索结果
    假如 一条物料审核结果为 block
    当 任意用户搜索
    那么 该物料不得出现在结果中
