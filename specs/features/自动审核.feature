# language: zh-CN
功能: 自动内容审核
  物料生成后自动送内容安全,违规拦截,零漏放。

  @REQ-501
  场景: 每条物料都有审核结果
    假如 生成了一条新物料
    当 系统触发审核
    那么 应写回审核结果 pass 或 review 或 block

  @REQ-502
  场景: 违规物料不可检索不可下载
    假如 一条物料审核结果为 block
    当 用户尝试检索或下载
    那么 系统应拒绝且该物料不出现在结果中

  @REQ-503
  场景: 审核超时不默认放行
    假如 一条物料的审核超时
    当 系统处理该超时
    那么 该物料应标记为 review 进人工复核
    而且 不得默认放行
