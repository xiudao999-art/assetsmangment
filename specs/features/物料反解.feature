# behave 可执行验收(闭环①的传感器)。跑法:.venv/bin/behave specs/features
# @REQ-id 标签 = 追溯脊柱,失败可回溯到 specs/物料反解.md 的对应需求。
# 现在是「红」状态(未实现),BMAD Dev 实现后转「绿」。

# language: zh-CN

功能: 视频反解物料
  作为运营用户,上传视频后系统自动反解成物料并送审。

  @REQ-201
  场景: 上传视频后自动反解并送审
    假如 我上传了一个 50MB 的 mp4 视频
    当 系统调用 Qwen-VL 反解
    那么 应生成至少 1 条物料
    而且 每条物料都带有审核结果
    而且 受理结果在 10 秒内返回

  @REQ-204
  场景: 送审失败时不丢弃
    假如 一条反解出的物料送审超时
    当 系统处理该失败
    那么 该物料应被标记为「待人工复核」
    而且 原视频应被保留
