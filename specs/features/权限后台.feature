# language: zh-CN
功能: 功能权限 RBAC 后台
  角色-权限-功能;后台可管、即时生效。

  @REQ-701
  场景: 无权限拒绝并审计
    假如 一个用户没有某功能权限
    当 该用户访问该功能
    那么 系统应拒绝并返回403
    而且 应记录审计

  @REQ-702
  场景: 后台调整权限即时生效
    假如 管理员在后台给角色新增了一个权限
    当 拥有该角色的用户再次访问
    那么 系统应即时放行
